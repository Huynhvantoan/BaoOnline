package com.toan_itc.baoonline.check_internet;

import android.content.Context;

/**
 * Created by Toan_Kul on 2/5/2015.
 */
public class Dialog_checkInternet {
    public Dialog_checkInternet(Context context){
        /*new MaterialDialog.Builder(context)
                .title(R.string.dialog_title_internet)
                .content(R.string.dialog_internet)
                .positiveText("Th? l?i")
                .callback(new MaterialDialog.ButtonCallback() {
                    @Override
                    public void onPositive(MaterialDialog dialog) {
                        super.onPositive(dialog);
                        dialog.dismiss();
                    }
                })
                .show();*/
    }
}
